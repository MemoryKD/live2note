"""LLM prompt templates for knowledge extraction."""

SYSTEM_PROMPT = """\
你是一位专业的知识整理助手。你的任务是从直播转写文本中提取结构化知识。

严格要求：
- 只提取原文中明确表达的信息，不得编造。
- 如果原文表述不清或信息不足，标注"原文表述不清"。
- 保持原文的核心观点和逻辑。
- 输出必须是合法的 JSON，不包含 markdown 代码块标记。"""

CHUNK_PROMPT_TEMPLATE = """\
请对以下直播转写内容进行结构化知识提取。

--- 转写内容 ---
{text}
--- 内容结束 ---

请以 JSON 格式输出，包含以下字段：
{{
  "summary": "2-3 句话的内容摘要",
  "key_points": ["核心观点1", "核心观点2", ...],
  "knowledge_points": ["可记录的知识点1", "知识点2", ...],
  "action_items": ["可执行建议1", "建议2", ...],
  "tags": ["标签1", "标签2", ...],
  "keywords": ["关键词1", "关键词2", ...],
  "important_quotes": ["原文重要引用1", "引用2", ...]
}}

注意：
- 如果某项信息在原文中没有体现，使用空数组 []。
- 如果整个内容信息量不足，summary 可以写"原文表述不清，信息量不足"。
- tags 最多 5 个，keywords 最多 10 个。
- important_quotes 选取最有价值的 1-3 句原文。"""
